
import {Header} from './header';

import { Products } from './products';

export const Home = () => {

    return (
        <div>
            <Header/>
            <Products />
        </div>
    );
}